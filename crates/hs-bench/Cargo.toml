[package]
name = "hs-bench"
description = "HAIRSPRING gate 8 (benchmark half): SWE-bench Verified runner - dataset loader, mission adapter, budget enforcement, report schema"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
hs-core = { path = "../hs-core" }
hs-log = { path = "../hs-log" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
