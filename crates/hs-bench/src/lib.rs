//! HAIRSPRING gate 8, benchmark half (spec section 10, row 8: benchmark
//! comparison vs baseline). Red proof tests in tests/bench_prep_proof.rs
//! define the contract. NO PAID MODEL CALLS: the offline proof runs on
//! in-repo fixtures with scripted patch sources.
