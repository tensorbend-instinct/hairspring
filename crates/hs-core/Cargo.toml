[package]
name = "hs-core"
description = "HAIRSPRING gate 1: canonical event schema, encoding, hash chain (spec section 3)"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
sha2 = "0.10"
uuid = { version = "1", features = ["v4", "serde"] }
